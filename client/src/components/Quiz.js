import React, { Component } from 'react';

import Spinner from './Spinner';

class Quiz extends Component {
    render() {
        return (
            <div>
                <Spinner />
            </div>
        )
    }
}

export default Quiz
