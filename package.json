{
  "name": "quizer",
  "version": "1.0.0",
  "description": "A quiz app",
  "main": "index.js",
  "scripts": {
    "server-dev": "cd server && nodemon index.js",
    "client-dev": "cd client && npm start",
    "start-dev": "concurrently -n 'server,client' -c 'red,green' \"npm run server-dev\" \"npm run client-dev\" "
  },
  "keywords": [
    "Quizer",
    "Quiz"
  ],
  "author": "Suresh Mangs",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.5"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.4"
  }
}
